class Card {
  constructor(id, title, description, columnId, userId) {
    this.id = id;
    this.title = title;
    this.description = description;
    this.columnId = columnId;
    this.userId = userId;
  }
}

export default Card;
